// Generated by CoffeeScript 1.9.2
(function() {
  describe('fmeCompileHtml', function() {
    beforeEach(inject(function($rootScope, $compile, $controller) {
      this.scope = $rootScope.$new();
      this.controller = $controller;
      this.compile = $compile;
      this.scope.description = '<p>test</p>';
      this.element = angular.element("<div fme-compile-html='true'  htmltext='description'> </div>");
      this.el = this.compile(this.element)(this.scope);
      return this.scope.$digest();
    }));
    return describe('element creation', function() {
      return it('sets the compiles the directive into a new div', function() {
        expect(this.el.scope().description).to.eq('<p>test</p>');
        return this.element.remove();
      });
    });
  });

}).call(this);
